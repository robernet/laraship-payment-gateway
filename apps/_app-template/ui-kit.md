# UI kit — <app>

The read-only **reference / resources provider** for this app — a source to borrow widgets, screens, and theme ideas from, NOT the app's skeleton. Claude reads it, takes only what a screen needs, and writes clean code in this app's own `lib/`.

@ui-catalog.md

> Imports the inventory generated by `/ui-catalog <app>`. Run it after vendoring the kit; inert until then.

## Kit
- Name: <kit name>
- Source: <url>
- Vendored path (read-only, out of build): `reference/<kit>/`  (Claude scans `reference/<kit>/lib`)
- License: <confirm the license covers this app's commercial use>

## Rules
- Reference / library model: keep this app's own architecture (Riverpod, Dio, freezed). Never import the kit's Bloc/Cubit or any `package:<kit>/...`.
- Borrow and adapt — take only what a screen needs; kits carry duplicated and unnecessary widgets.
- Pull across only the assets/packages a ported widget actually uses.
- Keep this app's `lib/` clean and deduplicated; treat the reference dir as read-only (re-vendor to update).

## What to mine (fill from the vendored source, then delete the brackets)
- Reusable widgets / helpers: <paths>
- Theme tokens: <path + accessors>
- Screens to borrow layouts from: <list>

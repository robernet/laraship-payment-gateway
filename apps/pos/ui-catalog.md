# UI catalog — pos

Auto-generated by `/ui-catalog pos` on 2026-08-09. Manual edits are overwritten on the next run.
Kit root scanned: `reference/devkit/lib` (DevKit — CodeCanyon, multi-demo-app kit: `ecommerce` + `food_delivery`).

## Theme tokens
- Colors: `reference/devkit/lib/config/apps/ecommerce/constant.dart` — `primaryColor` (0xFF07ac12), `assentColor` (accent, 0xFFe75f3f), `charcoal`, `blackGrey`, `softGrey`, `softBlue`. A second, differently-branded set lives in `config/apps/food_delivery/constant.dart` — pick one lineage, don't mix.
- Text/spacing styles: `reference/devkit/lib/config/apps/ecommerce/global_style.dart` — `GlobalStyle.appBarTitle`, `.productPrice`, `.sectionTitle`, `.detailProductPrice`, `.filterTitle`, plus `appBarElevation`/`appBarBackgroundColor`/`appBarSystemOverlayStyle` constants. Purely e-commerce naming (`productPrice`, `productSale`) — borrow the *pattern* (named TextStyle constants), not the names.
- Theme switching demo: `ui/feature/app_theme/` (4 prebuilt Material themes + a switcher) — not needed unless POS ever wants runtime theme switching.

## Reusable widgets
- `ButtonWidgetPage` — `ui/widget/form_component/button_widget.dart` — elevated/outlined/text button variants in one demo.
- `TextFieldWidgetPage` — `ui/widget/form_component/text_field_widget.dart` — styled input field variants (useful for login fields, cash-count entry).
- `DropdownWidgetPage` — `ui/widget/form_component/dropdown_widget.dart` — styled dropdown (candidate for a store/issuer picker).
- `AlertDialogPage` / `StandartDialogPage` — `ui/widget/dialog/{alert_dialog,standart_dialog}.dart` — confirm/cancel dialog shells, good fit for "confirm logout" / "confirm end of shift".
- `StandartCardPage` / `ElevationCardPage` — `ui/widget/card/{standart_card,elevation_card}.dart` — bordered/elevated card shells for summary tiles (shift totals, reference amount).
- `StandartDatatablePage` — `ui/widget/datatable/standart_datatable.dart` — row/column table, candidate for the shift transaction list.
- `AppBar1Page`…`AppBar4Page` — `ui/widget/app_bar/app_bar{1..4}.dart` — 4 app-bar styles (icon actions, gradient, centered title) — a natural home for a logout action icon.
- `StandartFabPage` / `FabWithLabelPage` — `ui/widget/fab/{standart_fab,fab_with_label}.dart` — FAB variants, one has a labeled/extended form (candidate for a prominent "End Shift" action).
- `StandartSnackbarPage` / `SnackbarWithActionPage` — `ui/widget/snackbar/{standart_snackbar,snackbar_with_action}.dart` — toast/snackbar with optional action button (e.g. "Logged out — Undo").
- `StandartDrawerPage` / `DrawerCustomHeaderPage` — `ui/widget/drawer/{standart_drawer,drawer_custom_header}.dart` — side-drawer shell with header, an alternate home for shift/logout actions instead of an app-bar menu.
- `BarcodeScanner1Page`…`BarcodeScanner3Page` — `ui/feature/barcode_scanner/barcode_scanner{1,2,3}.dart` — already ported into `barcode_scan_screen.dart`; kept here for reference if the scan UI needs revisiting.

## Shared helpers
- None found as standalone static helper classes outside the widget demo pages themselves — the kit favors per-widget demo pages over shared utility classes. `lib/library/` (15 files) holds small platform-utility wrappers (device/battery/storage info) — not applicable to POS screens.

## Screens by feature
- `ui/screen/signin -> ui/screen/signin (5)`: `Signin1Page`…`Signin4Page`, `SigninListPage` — closest analog to the operator login screen already built.
- `ui/screen/signup`, `ui/screen/forgot_password`, `ui/screen/verification` — auth flows POS doesn't need (single operator login only, no self-signup).
- `ui/screen/user_profile -> ui/screen/user_profile (4)`: `UserProfile1Page`…`UserProfile3Page`, `UserProfileListPage` — layout ideas for an operator/shift summary panel (avatar + stat rows), could inform the shift-close reconciliation screen.
- `ui/screen/home`, `ui/screen/product_list`, `ui/screen/timeline`, `ui/screen/notification`, `ui/screen/contact_us` — e-commerce/social specific, low relevance to POS.
- `ui/apps/ecommerce`, `ui/apps/food_delivery` — full demo-app assemblies; reference only for overall composition patterns, not component source.
- `ui/feature/* (36 folders, ~250 files)` — component-level demos (indicator 29, charts 18, shimmer_loading 13, bottom_navigation 13, page_route_animation 11, badges 11, banner_slider 8, etc.) — grep `ui/feature/<name>` directly when a specific effect is needed.

## Also available
- Cupertino: `ui/cupertino_widget/` (84 files) — iOS-style equivalents of most Material widgets above; only relevant if POS ever ships an iOS build.
- Form inputs: `ui/widget/form_component/` — text field, dropdown, checkbox, switch, radio, slider, date/time pickers, toggle button (11 files total).
- Charts: `ui/feature/charts/` (18 files) — bar/line/pie chart wrappers; candidate if a shift-totals dashboard is ever added.
- Loaders/shimmer: `ui/feature/shimmer_loading/` (13 files), `ui/feature/loading_animation/` (1 file), `ui/widget/progress_indicator/` — skeleton/spinner options for async states (reference lookup, shift open/close).
- i18n: `ui/feature/multi_language/` (6 files) — locale-switching demo; not currently a POS requirement (single-locale app per `apps/pos/CLAUDE.md`).
